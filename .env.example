# Wanderly Environment Variables
# Copy this file to .env or .env.local for local serverless development

# Google Gemini API Key (Server-Side Only)
# Do NOT prefix with VITE_ to keep it secret from the browser build
GEMINI_API_KEY=